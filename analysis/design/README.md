# What is the design?

This references that design decisions made for the pipeline. Included here is a script which collects those decisions (`design.R`) and a `.json` file which contains the study period dates (`study_dates.json`)