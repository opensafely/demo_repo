# What is the design?

This references that design decisions made for the pipeline. Included here is a script which collects those decisions (`design.R`), a script which defines the study periods (`study_dates.R`), and the results `.json` file (`study_dates.json`)
